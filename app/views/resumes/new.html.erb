<div class="container">
    <div class="row">
        <div class="col-xs-12 col-md-9 col-lg-8">
            <div class="panel panel-success">
                <div class="panel-heading ">
                    <h3> 回复信息:<%= @job.title %> </h3>
                </div>
                <%= simple_form_for [@job,@resume] do |f|%>
                    <%= f.input :content %>
                    <%= f.input :attachment %>
                    <%= f.submit '提交'  %>
                <% end %>
            </div>
        </div>
    </div>
</div>
